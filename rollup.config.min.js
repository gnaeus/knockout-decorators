import { default as uglifyPlugin } from "rollup-plugin-uglify";
import deepmerge from "deepmerge";
import config from "./rollup.config.js";

export default deepmerge(config, {
    dest: "dist/knockout-decorators.min.js",
    
    plugins: [
        uglifyPlugin()
    ]
});